# Prompt for a recurring Codex task

```text
Open X in the available signed-in Mac browser and search the latest posts with the query `(\"MiniMax H3\" OR \"Hailuo 3.0\" OR \"MiniMax-H3\") filter:native_video -filter:replies`. Discover original MiniMax H3 / Hailuo 3.0 video examples from the last 24 hours. Follow docs/DISCOVERY_WORKFLOW.md exactly. Put real video cases first. Do not publish directly or download media during discovery. Copy prompt text only when the original source post visibly publishes the exact text; otherwise record no prompt and mark the provenance `unknown`. Never generate, complete, translate into an alleged original, rewrite, adapt, reconstruct, reverse-engineer, summarize, or decompose a missing prompt, and never infer a hidden workflow from the finished video. Update data/candidates.json, deduplicate against existing candidates and cases, and use `mimo-v2.5-pro` only to classify public text and metadata when the local `MIMO_API_KEY` is configured. Do not ask the text model to produce prompt text. Use `mimo-v2.5` only for limited case and media verification of shortlisted videos, never for prompt reconstruction or workflow decomposition. Approved cases use the official X embed for in-site playback; only an explicit `approved-with-media-fallback` decision with documented redistribution permission may trigger a separate download and hosting step. Then give me a compact review table containing source, author, mode, public-prompt availability and provenance, factual classification, engagement snapshot, and confidence. If X is unavailable or logged out, report the blocker without inventing cases.
```
