# Prompt for a recurring Codex task

```text
Open X in the available signed-in Mac browser and search the latest posts with the query `(\"MiniMax H3\" OR \"Hailuo 3.0\" OR \"MiniMax-H3\") filter:native_video -filter:replies`. Discover original MiniMax H3 / Hailuo 3.0 video examples from the last 24 hours and follow docs/DISCOVERY_WORKFLOW.md exactly. Automatically publish clear cases whose original post explicitly identifies an H3-generated native video, has a clear author/source and playable media, and is not an obvious repost, bulk advertisement, tutorial/news clip, or unrelated output. Deduplicate against candidates and public cases, add clear cases directly to data/cases.json, cache their posters, and leave only genuinely ambiguous items in data/candidates.json. Copy prompt text only when the original source post visibly publishes the complete exact text; otherwise record `prompt: null` with provenance `not-published`. Never generate, complete, translate into an alleged original, rewrite, adapt, reconstruct, reverse-engineer, summarize, or decompose a missing prompt, and never infer a hidden workflow from the finished video. Use `mimo-v2.5-pro` only to classify public text and metadata when the local `MIMO_API_KEY` is configured; use `mimo-v2.5` only for limited eligibility/media verification of ambiguous videos. Approved cases use the official X embed for in-site playback; only an explicit `approved-with-media-fallback` decision with documented redistribution permission may trigger a separate download and hosting step. Run validation, tests, and the production build; after success commit and push the related changes to GitHub, allow Railway to auto-deploy, and verify deployment. Report published cases, remaining ambiguous items, public-prompt count, commit, and deployment status. If X is unavailable or logged out, report the blocker without inventing cases.
```
