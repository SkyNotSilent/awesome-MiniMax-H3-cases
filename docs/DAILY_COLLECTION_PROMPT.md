# Prompt for a recurring Codex task

```text
Open X in the available signed-in Mac browser and search the latest posts with the query `(\"MiniMax H3\" OR \"Hailuo 3.0\" OR \"MiniMax-H3\") filter:native_video -filter:replies`. Discover original MiniMax H3 / Hailuo 3.0 video examples from the last 24 hours. Follow docs/DISCOVERY_WORKFLOW.md exactly. Do not publish directly, do not download media during discovery, and do not infer hidden prompts. Update data/candidates.json, deduplicate against existing candidates and cases, and use `mimo-v2.5-pro` only for structured text classification when the local `MIMO_API_KEY` is configured. Use `mimo-v2.5` only for shortlisted video/audio verification. Approved cases use the official X embed for in-site playback; only an explicit `approved-with-media-fallback` decision with documented redistribution permission may trigger a separate download and hosting step. Then give me a compact review table containing source, author, mode, prompt provenance, classification, engagement snapshot, and confidence. If X is unavailable or logged out, report the blocker without inventing cases.
```
